<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <link href="https://fonts.googleapis.com/css2?family=Glory:ital,wght@0,100..800;1,100..800&display=swap" rel="stylesheet">
    <link asp-append-version="true" rel="stylesheet" href="~/assets/css/style.css">
    <link asp-append-version="true" rel="stylesheet" href="~/assets/css/Profilecss.css">
    <link asp-append-version="true" rel="stylesheet" href="~/assets/css/Image.css">
    @RenderSection("styles", required: false)
    <title>Document</title>
</head>
<body>
    <style>
        /* Custom styling for the login form */
        .modal-content {
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
        }

        .modal-header {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: #fff;
            padding: 1.5rem;
            border-top-left-radius: 15px;
            border-top-right-radius: 15px;
            text-align: center;
        }

            .modal-header h5 {
                font-weight: bold;
                font-size: 1.5rem;
                letter-spacing: 1px;
            }

        .modal-body {
            padding: 2rem;
        }

        /* Custom form styling */
        .form-control {
            border: 2px solid transparent;
            border-radius: 10px;
            padding: 0.75rem;
            background-color: #fff;
            transition: border-color 0.3s, box-shadow 0.3s;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
            margin-bottom: 1.5rem;
        }

            .form-control:focus {
                border-color: #007bff;
                box-shadow: 0 0 8px rgba(0, 123, 255, 0.6);
            }

        /* Label styling */
        .form-label {
            font-weight: 600;
            font-size: 1.1rem;
            color: #495057;
        }

        /* Custom checkbox */
        .form-check-input {
            border-radius: 50%;
            width: 18px;
            height: 18px;
        }

        /* Animated button */
        .btn-primary {
            background-color: #007bff;
            border: none;
            border-radius: 10px;
            padding: 0.75rem 1.5rem;
            font-weight: bold;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: background-color 0.4s, transform 0.3s;
        }

            .btn-primary:hover {
                background-color: #0056b3;
                transform: translateY(-3px);
                box-shadow: 0 8px 20px rgba(0, 123, 255, 0.4);
            }

        /* Icons inside input fields */
        .input-group {
            position: relative;
        }

            .input-group i {
                position: absolute;
                top: 50%;
                left: 10px;
                transform: translateY(-50%);
                color: #007bff;
            }

            .input-group input {
                padding-left: 40px;
            }

        /* Responsive modal fade-in */
        .modal.fade .modal-dialog {
            transform: translateY(-60px);
            opacity: 0;
            transition: all 0.6s ease-in-out;
        }

        .modal.show .modal-dialog {
            transform: translateY(0);
            opacity: 1;
        }

        /* Footer with custom links */
        .modal-footer {
            border-top: none;
            padding-top: 0;
        }

            .modal-footer small {
                font-size: 0.9rem;
            }

            .modal-footer a {
                color: #007bff;
                text-decoration: none;
                transition: color 0.3s;
            }

                .modal-footer a:hover {
                    color: #0056b3;
                    text-decoration: underline;
                }

        .btn-primary:hover {
            background-color: #0056b3;
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0, 123, 255, 0.4);
        }

        .modal-backdrop {
            background-color: rgba(0, 0, 0, 0.5); /* dimmed background */
            transition: backdrop-filter 0.4s ease;
            backdrop-filter: blur(0px);
        }

        .modal.show ~ .modal-backdrop {
            backdrop-filter: blur(5px);
        }

        .google-btn {
            background-color: white;
            color: #757575;
            border: 1px solid #ddd;
            padding: 0.75rem;
            border-radius: 10px;
            font-weight: bold;
            letter-spacing: 0.5px;
            display: flex;
            align-items: center;
            justify-content: center;
            text-transform: uppercase;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
            margin-bottom: 1rem;
            width: 100%;
        }

            .google-btn:hover {
                background-color: #f7f7f7;
                box-shadow: 0 8px 15px rgba(0, 0, 0, 0.1);
            }

            .google-btn i {
                margin-right: 10px;
                font-size: 1.2rem;
                color: #4285f4;
            }

        .register-btn {
            background-color: transparent;
            border: 2px solid #007bff;
            color: #007bff;
            padding: 0.75rem;
            border-radius: 10px;
            font-weight: bold;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            transition: color 0.3s ease, border-color 0.3s ease, background-color 0.3s ease;
            width: 100%;
        }

            .register-btn:hover {
                background-color: #007bff;
                color: white;
                border-color: #007bff;
            }

        .buttons-container {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
    </style>
    @await Component.InvokeAsync("SettingHeader")

    <main class="mb-5">
        <div class="modal fade" id="loginModal" tabindex="-1" aria-labelledby="loginModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h5 class="modal-title" id="loginModalLabel">Welcome Back!</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <form id="loginForm" asp-controller="Account" asp-action="Login" method="post">
                            <!-- Email input with icon -->
                            <div class="input-group mb-3">
                                <i class="fa fa-envelope"></i>
                                <input type="email" class="form-control" id="Input_Email" name="Email" required placeholder="Email Address">
                            </div>
                            <!-- Password input with icon -->
                            <div class="input-group mb-3">
                                <i class="fa fa-lock"></i>
                                <input type="password" class="form-control" id="Input_Password" name="Password" required placeholder="Password">
                            </div>
                            <!-- Remember me checkbox -->
                            <div class="mb-3 form-check">
                                <input type="checkbox" class="form-check-input" id="Input_RememberMe" name="RememberMe">
                                <label class="form-check-label" for="Input_RememberMe">Remember Me</label>
                            </div>
                            <!-- Animated submit button -->
                            <button type="submit" class="btn btn-primary w-100 mb-3">Login</button>

                            <!-- Google Sign-In and Register buttons -->
                            <div class="buttons-container">
                                <!-- Google Sign-In Button -->
                                <button type="button" class="google-btn">
                                    <i class="fab fa-google"></i> Sign in with Google
                                </button>

                                <!-- Register Button -->
                                <a href="/Account/Register" class="register-btn text-center">Register Here</a>
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <small class="text-muted">Forgot your password? <a href="#">Reset here</a></small>
                    </div>
                </div>
            </div>
        </div>
        @RenderBody()
    </main>
  @await Component.InvokeAsync("SettingFooter")
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script asp-append-version="true" src="~/assets/js/main.js"></script>
    <script asp-append-version="true" src="~/assets/js/Basket.js"></script>

    <!--Start of Tawk.to Script-->
    <script asp-append-version="true" type="text/javascript">
        var Tawk_API = Tawk_API || {}, Tawk_LoadStart = new Date();
        (function () {
            var s1 = document.createElement("script"), s0 = document.getElementsByTagName("script")[0];
            s1.async = true;
            s1.src = 'https://embed.tawk.to/66e887e950c10f7a00ab4437/1i7u55qhk';
            s1.charset = 'UTF-8';
            s1.setAttribute('crossorigin', '*');
            s0.parentNode.insertBefore(s1, s0);
        })();
    </script>
    @RenderSection("scripts", required: false)
    <!--End of Tawk.to Script-->
</body>
</html>
